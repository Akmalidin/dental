Django==5.1.4
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
django-filter==24.3
drf-spectacular==0.27.2
django-simple-history==3.7.0
django-cors-headers==4.6.0
whitenoise==6.8.2
Pillow==11.0.0
openpyxl==3.1.5
python-decouple==3.8
qrcode==8.0
waitress==3.0.2
faster-whisper==1.2.1
# Используется в config/urls.py (продакшн-urlconf, path("rosetta/", ...)) —
# без неё ROOT_URLCONF=config.urls вообще не импортируется. Нужна и здесь,
# не только в requirements_prod.txt: тест apps.notifications.tests.
# ServiceWorkerProductionRoutingTestCase явно переключается на этот
# urlconf (override_settings), чтобы проверять именно тот файл, где был
# баг с отсутствующими /sw.js и /manifest.json (см. коммит).
django-rosetta==0.10.1
